// Collecting Enums here for now

public enum ActionState
{ 
    Alerted, 
    Cautious, 
    Distracted, 
    Searching, 
    Idle, 
    GoingBack 
};

public enum VisionState
{ 
    Red, 
    Yellow, 
    White 
};

public enum LevelState
{
    Unlocked,
    Locked,
    Completed,
    Selected
}